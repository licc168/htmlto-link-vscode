{
  "name": "htmlto-link-vscode",
  "displayName": "Quick Static Site Publish",
  "description": "一键发布本地静态网站文件夹到 htmlto.link，自动识别入口 HTML，支持游客模式、Token 发布，并立即返回公开访问链接。",
  "version": "0.1.2",
  "publisher": "licc",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:htmlToLink.openPanel",
    "onCommand:htmlToLink.deployFolder",
    "onCommand:htmlToLink.setToken",
    "onCommand:htmlToLink.clearToken",
    "onCommand:htmlToLink.openLastDeployUrl"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "htmlToLink.openPanel",
        "category": "静态网站一键发布",
        "title": "打开发布面板",
        "icon": "$(rocket)"
      },
      {
        "command": "htmlToLink.deployFolder",
        "category": "静态网站一键发布",
        "title": "选择文件夹并发布",
        "icon": "$(cloud-upload)"
      },
      {
        "command": "htmlToLink.setToken",
        "category": "静态网站一键发布",
        "title": "打开 Token 设置",
        "icon": "$(key)"
      },
      {
        "command": "htmlToLink.clearToken",
        "category": "静态网站一键发布",
        "title": "清除已保存 Token",
        "icon": "$(trash)"
      },
      {
        "command": "htmlToLink.openLastDeployUrl",
        "category": "静态网站一键发布",
        "title": "打开最近发布链接",
        "icon": "$(link-external)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "htmlToLink.deployFolder",
          "when": "explorerResourceIsFolder",
          "group": "navigation@90"
        }
      ]
    },
    "configuration": {
      "title": "静态网站一键发布",
      "properties": {
        "htmlToLink.apiBaseUrl": {
          "type": "string",
          "default": "https://htmlto.link",
          "description": "htmlto.link 部署接口的基础地址。"
        },
        "htmlToLink.autoCopyUrl": {
          "type": "boolean",
          "default": true,
          "description": "部署成功后自动复制链接。"
        },
        "htmlToLink.defaultEntryFile": {
          "type": "string",
          "default": "index.html",
          "description": "自动识别入口文件时优先使用的文件名。"
        },
        "htmlToLink.excludePatterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "node_modules/**",
            ".git/**",
            ".next/**",
            "dist/cache/**",
            ".turbo/**"
          ],
          "description": "打包上传时默认排除的目录或文件模式。"
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "check": "tsc -p . --noEmit",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.2.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
